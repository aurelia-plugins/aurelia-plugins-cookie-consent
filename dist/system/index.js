'use strict';

System.register(['./aurelia-cookie-consent'], function (_export, _context) {
  "use strict";

  return {
    setters: [function (_aureliaCookieConsent) {
      var _exportObj = {};

      for (var _key in _aureliaCookieConsent) {
        if (_key !== "default" && _key !== "__esModule") _exportObj[_key] = _aureliaCookieConsent[_key];
      }

      _export(_exportObj);
    }],
    execute: function () {}
  };
});